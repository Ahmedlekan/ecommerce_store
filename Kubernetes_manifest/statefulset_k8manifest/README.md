# Statefulset Mode

Use these manifests when a service should run with in-cluster stateful dependencies.

Examples:

- in-cluster MySQL for `catalog`
- in-cluster PostgreSQL and RabbitMQ for `orders`

These manifests are preserved as an alternative deployment mode.

