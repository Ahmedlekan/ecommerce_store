# HTTP Retail Store Manifests

This folder is split by deployment mode.

- `statefulset/`: in-cluster stateful backing services
- `aws_dataplane/`: AWS-managed backing services

Pick one mode per service when deploying.

