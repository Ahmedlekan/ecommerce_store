# AWS Project Area

This folder contains the AWS-specific infrastructure, Kubernetes manifests,
observability configuration, GitOps configuration, Helm assets, and AIOps work
for the ecommerce platform.

Shared application source code remains at the repository root in
`Application Code/` so the same services can be deployed to other cloud
providers.
